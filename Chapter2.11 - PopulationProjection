# Introduction-to-Java-Programming
import java.util.*;
public class PopulationProjection {
/*
(Population projection) Rewrite Programming Exercise 1.11 to prompt the user
to enter the number of years and display the population after the number of years.
*/
	public static void main(String[] args) {
		Scanner input = new Scanner(System.in);
		
		System.out.print("Enter number of years: ");
		double years = input.nextDouble();
		
		double birthInYear = (365 * 24 * 60 * 60) / 7;
		double deathInYear = (365 * 24 * 60 * 60) / 13;
		double immigrantInYear = (365 * 24 * 60 * 60) / 45;
		
		double initialPopulation = 312032486;
		
		long newPopulation = (long) (initialPopulation + years * (birthInYear + immigrantInYear - deathInYear));
		
		System.out.print("The population in " + years + " years is " + newPopulation);
	}
}
